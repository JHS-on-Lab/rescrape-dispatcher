# ================================================================
# prod 환경 — APP_ENV=prod
# .env 에서 달라지는 것만 작성한다. (VPC 내부, SSH 터널 불필요)
# RDS 접속 정보는 배포 시 환경변수로 주입한다.
# ================================================================

SOLR_MAX_DOCS=5000
WORKER_ID=rescrape-prod-1
